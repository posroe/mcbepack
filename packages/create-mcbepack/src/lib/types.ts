import type { FileToCreate } from "../schema/file.js";
import type { PackageManager } from "../schema/package-manager.js";
import type {
    ProjectConfig,
    ProjectDependency,
    ProjectUuids,
    ScriptConfig
} from "../schema/project.js";

export type MinecraftPackageName = string;
export type { FileToCreate, PackageManager, ProjectConfig, ProjectDependency, ProjectUuids, ScriptConfig };
