{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "bun run --filter '*' build",
        "dev": "bun run --filter '*' dev"
    },
    "dependencies": {
        "@types/node": "^25.3.0",
        "chokidar": "^5.0.0",
        "dotenv": "^17.2.3",
        "picocolors": "^1.1.1",
        "yargs": "^18.0.0"
    },
    "devDependencies": {
        "@types/yargs": "^17.0.35"
    }
}